import { ForbidPastingDirective } from './forbid-pasting.directive';

describe('ForbidPastingDirective', () => {
  it('should create an instance', () => {
    const directive = new ForbidPastingDirective();
    expect(directive).toBeTruthy();
  });
});
