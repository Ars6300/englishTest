import { ButtonsStylingDirective } from './buttons-styling.directive';

describe('ButtonsStylingDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonsStylingDirective();
    expect(directive).toBeTruthy();
  });
});
